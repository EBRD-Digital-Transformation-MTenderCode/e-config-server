#Application name
spring.application.name: config-server

#Application port
server.port: 8888

#SSH connection using public SSH key (cat ~/.ssh/id_rsa.pub)
spring.cloud.config.server.git.uri: git@bitbucket.org:main_/public-config-repo.git

#Logging level
logging.level.ROOT: ERROR